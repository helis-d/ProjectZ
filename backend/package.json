{
  "name": "projectz-backend",
  "version": "1.0.0",
  "description": "Nakama TypeScript Backend Module for ProjectZ",
  "main": "build/index.js",
  "scripts": {
    "build": "npx tsc -p tsconfig.json",
    "type-check": "npx tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "nakama-runtime": "github:heroiclabs/nakama-common"
  }
}
